# This is the root .env.example file
# Copy this to .env and fill in your values
# Each app has its own .env.example file with more specific configurations

# Global Configuration
NODE_ENV=development
